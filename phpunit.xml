<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>

        <!--
            PostgreSQL, not SQLite.

            The correctness of this system rests on things SQLite cannot express:
            the bookings EXCLUDE constraint that makes double-booking impossible
            (ADR-002), the trigger that makes the ledger append-only (ADR-003),
            partial unique indexes, and timestamptz. Running the suite on SQLite
            would silently skip every one of them and report green.

            Uses a separate database on the same server as development.
        -->
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_HOST" value="mcars-pgsql-1"/>
        <env name="DB_PORT" value="5432"/>
        <env name="DB_DATABASE" value="mcars_testing"/>
        <env name="DB_USERNAME" value="mcars"/>
        <env name="DB_PASSWORD" value="secret"/>
        <env name="DB_URL" value=""/>

        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="APP_TIMEZONE" value="Africa/Algiers"/>
        <env name="BRANCHES_ENABLED" value="false"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
